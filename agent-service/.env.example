# Copy to .env (gitignored) and fill in. Never commit real values.

OPENAI_API_KEY=
# Model string per master instructions (overrides the spec's placeholder —
# see DESIGN.md open questions). Never printed or logged anywhere in code.
MODEL_NAME=gpt-5.6-luna
